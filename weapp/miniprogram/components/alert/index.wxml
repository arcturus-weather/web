<view class="alert-container alert-class" style="background-color: {{backgroundColor}};">
  <!-- 描述 -->
  <view
    style="color: {{color}}"
    class="content"
    >{{text}}</view
  >
  <!-- 右图标 -->
  <common name="{{rightIcon}}" color="{{color}}" />
</view>
