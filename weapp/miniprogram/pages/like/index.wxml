<!-- 导航栏 -->
<nav-bar navLeftIcon="{{isPC ? '' : 'back'}}" navTitle="收藏城市" bind:left="back" textColor="#000" />

<scroll-view
  class="like-container"
  style="padding-top: {{paddingTop}}px"
  scroll-y
  enhanced="{{true}}"
  show-scrollbar="{{false}}"
>
  <view class="search-btn-container">
    <view class="search-btn" bindtap="searchPage">
      <common name="search" color="#909399" />
      <view class="search-text">点击搜索</view>
    </view>
  </view>
  <button class="add-btn" hover-class="none" bindtap="searchPage">
    <common name="add" size="35" color="#909399" />
  </button>
</scroll-view>
