import { BrowserRouter } from 'react-router-dom';
import Router from '@src/router';

export default function App() {
  return (
    <BrowserRouter>
      <Router></Router>
    </BrowserRouter>
  );
}
